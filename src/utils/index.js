const { blackGreen, green } = require('./colors.js')
const { filter } = require('./filter.js')
const { forEach } = require('./forEach.js')

module.exports = {
  blackGreen,
  green,
  filter,
  forEach
}